{
  "name": "license-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Online License Management System",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "db:migrate": "pnpm --filter @license-manager/database db:migrate",
    "db:studio": "pnpm --filter @license-manager/database db:studio"
  },
  "keywords": ["license", "saas", "management"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
